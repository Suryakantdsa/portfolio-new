/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "duramix-erp-dev.s3.ap-south-1.amazonaws.com",
    ],
  },
};

export default nextConfig;
